# @todo clean this and other config/yml files
imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

# Put parameters here that don't need to change on each machine where the app is deployed
# http://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: en

framework:
    #esi:             ~
    #translator:      { fallbacks: ["%locale%"] }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    #serializer:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"
#    exception_controller: 'AppBundle\Controller\UsersController::exceptionAction'

# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    bundles:        [ ]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: "%kernel.root_dir%/Resources/java/compiler.jar"
        #yui_css:
        #    jar: "%kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar"

# Doctrine Configuration
doctrine:
    dbal:
        driver:   pdo_mysql
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  utf8mb4
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: "%kernel.root_dir%/data/data.db3"
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #     path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true

# Swiftmailer Configuration
#swiftmailer:
#    transport: "%mailer_transport%"
#    host:      "%mailer_host%"
#    username:  "%mailer_user%"
#    password:  "%mailer_password%"
#    spool:     { type: memory }

sensio_framework_extra:
    view:   { annotations: false }
#    router: { annotations: true }

fos_rest:
#    body_listener:
#        array_normalizer: fos_rest.normalizer.camel_keys
#        decoders:
#            json: fos_rest.decoder.json
    param_fetcher_listener: true
    format_listener:
        rules:
            # html requires creating a template
            - { priorities: [json, xml], fallback_format: json, prefer_extension: true }
    routing_loader:
        default_format: json
#    access_denied_listener:
#        json: true
    view:
        # to return data instead of Response/JsonResponse object
        view_response_listener: force
        exception_wrapper_handler: 'AppBundle\Handler\ExceptionWrapperHandler'
#    service:
#        exception_handler:    fos_rest.view.exception_wrapper_handler
#        exception_handler: 'AppBundle\Handler\ExceptionWrapperHandler'
    exception:
        enabled: true
        codes:
#            'Symfony\Component\HttpKernel\Exception\NotFoundHttpException': 405
#            'Symfony\Component\Routing\Exception\ResourceNotFoundException': 404
#            'Doctrine\DBAL\DBALException': 501
            'AppBundle\Exception\GameFlowException': HTTP_UNPROCESSABLE_ENTITY
            'AppBundle\Exception\InvalidShipsException': HTTP_BAD_REQUEST
            'Symfony\Component\Security\Core\Exception\AuthenticationException': HTTP_UNAUTHORIZED
        messages:
#            'Symfony\Component\HttpKernel\Exception\NotFoundHttpException': false
#            'Doctrine\DBAL\DBALException': true
            'AppBundle\Exception\GameFlowException': true
            'AppBundle\Exception\InvalidShipsException': true
            'Symfony\Component\Security\Core\Exception\AuthenticationException': true
